version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ENV=development
      - ALLOWED_ORIGIN=http://localhost:3000
    restart: unless-stopped
