name: Deploy Inbound-Scanner

on:
  push:
    branches: [main]
    paths:
      - 'lambdas/inbound-scanner/**'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-northeast-2
  LAMBDA_FUNCTION_NAME: smartscan-inbound
  LAMBDA_DIRECTORY: lambdas/inbound-scanner

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run tests before deploy
        run: |
          pip install pytest supabase resend boto3 --quiet
          PYTHONPATH=lambdas/inbound-scanner pytest lambdas/inbound-scanner/tests/ -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-2

      - name: Package and Deploy
        run: |
          cd lambdas/inbound-scanner
          pip install -r requirements.txt -t ./package --quiet
          cp -r ../lambda_shared ./package/
          cp -r common ./package/
          cp -r repositories ./package/
          cp -r services ./package/
          cp lambda_function.py ./package/
          cd package && zip -r ../function.zip . -q
          aws lambda update-function-code \
            --function-name ${{ env.LAMBDA_FUNCTION_NAME }} \
            --zip-file fileb://../function.zip
